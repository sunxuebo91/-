<view class="page">
  <view class="content">
    <!-- 头像区域 -->
    <view class="avatar-section">
      <button class="avatar-btn" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
        <image class="avatar" src="{{avatarUrl || me.avatarUrl || '/images/icons/avatar.png'}}"></image>
      </button>
      <view class="avatar-tip">点击设置头像</view>
    </view>

    <!-- 表单区域 -->
    <view class="form-section">
      <!-- 昵称输入框 -->
      <view class="form-control">
        <input type="nickname" class="form-input" placeholder="请输入昵称" value="{{nickname}}" bindinput="onNicknameInput" />
      </view>

      <!-- 登录按钮 -->
      <view class="form-control">
        <button class="form-button" open-type="getPhoneNumber" bindgetphonenumber="onGetPhoneNumber">
          手机号快捷登录
        </button>
      </view>
    </view>

    <!-- 协议区域 -->
    <view class="agreement">
      <checkbox class="checkbox" checked="{{agreed}}" bindtap="toggleAgree" />
      <text class="text">我已阅读并同意</text>
      <text class="link" bindtap="openUserAgreement">《用户协议》</text>
      <text class="text">和</text>
      <text class="link" bindtap="openPrivacyPolicy">《隐私政策》</text>
    </view>
  </view>
</view>

